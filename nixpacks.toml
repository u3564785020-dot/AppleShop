[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["chmod +x node_modules/.bin/* || true", "npm run build"]

[start]
cmd = "npx serve -s build -l $PORT"

