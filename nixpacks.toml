[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps", "chmod -R +x node_modules/.bin/ || true"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node node_modules/serve/build/main.js -s build -l ${PORT:-3000}"

